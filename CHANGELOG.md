# Changelog — UntitledPoolGame

Toutes les modifications notables apportées au projet sont listées ici, dans l'ordre
chronologique inverse (les plus récentes en haut).

## 2026-08-24
- Mise en place de la structure de dossiers `Assets/` : `Scripts/{Player,Pool,Dimensions,Combat,NPC,Core,UI}`, `Prefabs/`, `Materials/`, `Art/`, `Audio/`, `ScriptableObjects/Dimensions/`.
- Création de `TODO.md` et `CHANGELOG.md` à la racine du projet pour suivre l'avancement.
- Décision : le contrôleur FPS partira d'un plugin existant que l'utilisateur installe lui-même ; prochaine étape après installation = évaluer son adéquation, puis enchaîner sur la mécanique de billard (tenue/pose de la queue, tir, physique des billes).
- Précisions gameplay billard : interaction de proximité avec la bille blanche déclenche un mode visée dédié (caméra sur le bout de la queue, point de contact déplaçable sur la bille pour les effets, prévisualisation de trajectoire avant tir). Le système doit rester modulaire car des dimensions pourront le détourner radicalement (ex : joueur miniaturisé sur la table, queue = arme, billes = ennemis, façon shooter).
- Précision projet : le jeu doit supporter le multijoueur en ligne en plus du multijoueur local. Ajouté comme décision d'architecture prioritaire à trancher avant d'avancer trop loin sur le contrôleur FPS et la mécanique de billard, car ça conditionne les choix techniques (plugin FPS compatible réseau, synchronisation de la physique des billes, etc.).
- Plugin FPS installé par l'utilisateur : **Physics Character Controller** de Nappin (`Assets/Plugins/nappin/PhysicsCharacterController`). Analyse du code (`CharacterManager.cs`, `InputReader.cs`) : contrôleur Rigidbody local, sans réseau intégré, mais architecture input/simulation découplée compatible avec un modèle réseau owner-authoritative (pas de réécriture nécessaire, juste ajouter la synchro réseau par-dessus).
- Décision réseau tranchée : **Netcode for GameObjects**. Ajout de `com.unity.netcode.gameobjects: 2.7.0` dans `Packages/manifest.json` (compatible Unity 6000.2.14f1). Reste à câbler sur le prefab joueur et tester.
- Clarification : la compatibilité réseau du plugin Nappin n'a été vérifiée que par lecture de code, pas par test en direct (éditeur déjà ouvert par l'utilisateur, pas de test lancé côté agent). Décision : tester d'abord le contrôleur en solo avant d'ajouter le réseau. Scène de démo déjà fournie par le plugin identifiée pour ça : `PCC_Gym_Humanoid.unity` (et `PCC_Gym.unity`), toutes deux déjà câblées avec joueur/caméra/input et un parcours de test (pentes, marches, murs, échelles).
- Utilisateur a ajouté `com.unity.cinemachine: 3.1.7` (nécessaire pour la caméra du contrôleur Nappin).
- Fix : suppression de `Assets/Start.cs`, script gabarit par défaut de Unity vide et cassé (`error CS0542` — une méthode ne peut pas porter le même nom que sa classe englobante), bloquait toute compilation.
- Fix : ajout de `com.unity.splines: 2.8.4` au manifest — Cinemachine 3.1.7 référence `UnityEngine.Splines.SplineComponent` dans son éditeur, absent du projet, ce qui provoquait un `TypeLoadException` à l'ouverture de la scène de test.
- Utilisateur a configuré le joueur en vue première personne (`Is Third Person Default` décoché sur `[Cameras]`) et validé le contrôleur en solo — fonctionnel, réglages fins à revoir plus tard.
- Décision : câbler Netcode for GameObjects sur le joueur avant d'attaquer la mécanique de billard.
- Constat technique important : le rig caméra/input Nappin (`[Cameras]` + `[InputSystem]`) est séparé du prefab joueur et placé une fois dans la scène (câblé par référence Inspector) — incompatible tel quel avec un spawn réseau dynamique (un prefab ne peut pas référencer un objet de scène). Ajout de trois scripts pour résoudre ça sans réorganiser les prefabs Nappin : `Assets/Scripts/Core/LocalClientRig.cs` (singleton exposant le rig caméra/input local du client), `Assets/Scripts/Player/NetworkPlayerRigLink.cs` (bind au spawn si `IsOwner`, sinon désactive `CharacterManager`), `Assets/Scripts/Core/NetworkBootstrap.cs` (boutons Host/Client/Server pour tester). Câblage manuel restant à faire dans l'éditeur (voir TODO.md).
- Fix : ajout de `com.unity.multiplayer.playmode: 1.6.3` — absent du projet, donc `Window > Multiplayer` n'affichait que "Multiplayer Center" et pas "Multiplayer Play Mode" (nécessaire pour tester plusieurs clients dans un seul éditeur).
- Diagnostic réseau : `NetworkManager` sans transport assigné (`No transport has been selected!`) — fix en assignant `Unity Transport` dans le champ `Network Transport`. Découverte que le setup avait été fait dans `Assets/Scenes/SampleScene.unity` avec des copies de travail des prefabs Nappin dans `Assets/Test/`, et qu'un Prefab Variant `Assets/Prefabs/Player/[Player] Variant.prefab` avait été créé par l'utilisateur pour porter les composants réseau sans modifier les fichiers du plugin — bonne pratique, gardée telle quelle.
- Fix : `NullReferenceException` dans `CameraManager.Start()` — s'exécutait automatiquement au chargement de la scène avant que `NetworkPlayerRigLink` n'ait eu la chance de remplir `characterManager`. Résolu en désactivant le composant `CameraManager` par défaut sur `[Cameras]` (notre script l'appelle explicitement au bon moment).
- Fix : caméra qui ne suivait pas le joueur — les cibles `Follow` des `CinemachineCamera` (FirstPerson/ThirdPerson) n'étaient pas préréglées dans le prefab `[Cameras]` (câblées à la main dans la démo non-réseau originale). `NetworkPlayerRigLink` les réassigne maintenant dynamiquement au spawn (`headPoint` / `characterModel`).
- Bug persistant malgré plusieurs correctifs : le strafe latéral (Q/D) faisait tourner le corps du joueur vers la direction de déplacement au lieu de le faire glisser sur le côté en gardant le regard fixe sur la caméra — comportement "3e personne" (`MoveRotation()`) au lieu du comportement FPS attendu (`ForceRotation()`), malgré `Is Third Person Default` correctement à `false` et un forçage explicite de `SetLockToCamera(true)`. Cause exacte non identifiée avec certitude après plusieurs hypothèses vérifiées et écartées (bindings clavier positionnels corrects pour AZERTY, contraintes Rigidbody normales, pas de doublon de caméra `MainCamera`).
- **Décision** : abandon du contrôleur Nappin pour le mouvement du joueur, remplacé par un contrôleur FPS maison écrit de zéro (`Assets/Scripts/Player/FpsPlayerController.cs`) — `CharacterController` Unity standard, mouvement calculé directement depuis le `transform` du joueur (strafe garanti correct par construction), rotation pilotée directement par la souris/manette, utilise les actions Move/Look déjà présentes dans `Assets/InputSystem_Actions.inputactions` (couvrant nativement clavier ZQSD et manette). Reste réseau-aware (IsOwner) comme le système précédent.
- Prefab joueur construit et câblé dans l'éditeur (CharacterController + NetworkObject + NetworkTransform Owner + FpsPlayerController + CameraPivot), assigné comme Player Prefab du NetworkManager. Testé en Host/Client via Multiplayer Play Mode : mouvement, strafe latéral et caméra fonctionnent correctement.
- Fix : les champs `InputActionReference` (Move/Look) se vidaient à chaque réouverture du prefab (bug de persistance connu d'Unity sur ce type de référence). Remplacés par un seul champ `InputActionAsset` (référence stable vers le fichier entier), actions retrouvées par nom au runtime.
- Fix : chaque joueur voyait son propre corps flotter devant sa caméra (modèle hérité du prefab Nappin jamais caché) — donnait l'impression d'un "joueur en double" qui imitait exactement ses mouvements. Ajout d'un masquage des `Renderer` du joueur pour son propre propriétaire dans `OnNetworkSpawn()`.
- Fix : un vrai doublon existait bel et bien en plus de ça — une instance de `Player` posée directement dans `SampleScene.unity` (oubliée pendant la construction du prefab), non spawnée via le réseau donc jamais masquée ni synchronisée, lisant le même input local que la machine et imitant le joueur. Supprimée de la scène.
- Contrôleur FPS multijoueur maison validé fonctionnel de bout en bout (mouvement, strafe, caméra, un seul joueur visible par écran comme attendu).

## 2026-08-31
- Demande utilisateur : implémenter les règles de base du billard, en pensant dès maintenant à la structure pour les pouvoirs/billes spéciales à venir (sans les implémenter tout de suite).
- `PoolBall.cs` étendu : champ `Number`, propriété calculée `Group` (`BallGroup.Cue/Solid/Eight/Stripe`, sans cas "Special" — une bille spéciale sera un composant marqueur séparé plus tard, comme `Cue.cs`), registre statique `Active` (`OnEnable`/`OnDisable`), méthode statique `AnyMoving()`, événement statique `Pocketed` déclenché en tête de `OnPocketed()` — accroche pour un futur système de pouvoirs.
- Nouveau `Assets/Scripts/Pool/PoolMatchRules.cs` : règles 8-ball simplifiées à 2 joueurs (attribution de groupe à la première bille légale empochée, tour qui continue sur bille du bon groupe, scratch sur bille blanche empochée, victoire/défaite sur la bille 8 selon si le groupe est nettoyé), plus un affichage `OnGUI` basique. Écrit comme `MonoBehaviour` simple, ni réseauté ni "Local" — n'observe que l'état des billes via `PoolBall`, donc valable tel quel en ligne et hors-ligne sans portage.
- `PoolTableBuilder.cs` : `CreateBall` accepte maintenant un paramètre `number` (assigné à chaque bille du rack et à la bille blanche via `SerializedObject`, même principe que pour `isCueBall`), et `BuildTable()` ajoute `PoolMatchRules` sur la racine `PoolTable` — vaut pour les deux variantes (Online/Offline), le composant est network-agnostic.
- Demande de suivi : ajouter le 9-ball, le 14.1 (straight pool) et un mode "Party" (pouvoirs à venir), choisis via un écran de sélection en début de partie. Décisions de scope validées avec l'utilisateur (toutes les options recommandées/casual) : pas d'annonce de poche (call-shot) pour le 8-ball/14.1, pas de re-rack pour le 14.1 (juste un score cible), Party = stub sur les règles du 8-ball sans pouvoir branché.
- Extraction de la logique 8-ball dans `EightBallRuleSet.cs`, derrière une nouvelle interface `IPoolRuleSet.cs` (`Setup`/`ResolveShot`/`DescribePlayer`) et un enum `PoolGameMode.cs` (`EightBall/NineBall/FourteenOne/Party`) — ajouter un mode revient à ajouter une implémentation, sans toucher `PoolMatchRules.cs`.
- Ajout de `NineBallRuleSet.cs` (seules les billes 1-9 + la blanche restent en jeu, désactivation des billes 10-15 à `Setup` ; pas de vérification de contact sur la bille la plus basse — hors scope ; empocher la bille "suivante" dans l'ordre garde la main, empocher la 9 gagne immédiatement sauf scratch simultané) et `FourteenOneRuleSet.cs` (pas de groupe ni de re-rack, chaque bille légale empochée vaut 1 point, premier au score cible gagne).
- `PoolMatchRules.cs` retravaillé en coquille légère : garde la boucle de détection de tir et l'affichage `OnGUI`, délègue tout le reste au `IPoolRuleSet` sélectionné. Ajout d'un écran de sélection de mode (`OnGUI`, boutons pour les 4 modes + champ score cible pour le 14.1 + bouton pour démarrer) affiché avant que la partie ne commence réellement à suivre les billes/tours.
- Aucun changement nécessaire dans `PoolTableBuilder.cs` pour cette passe — pas de rack dédié losange pour le 9-ball (les billes 10-15 sont juste désactivées après le rack triangle habituel), limitation connue et assumée pour l'instant.
- Bug retour utilisateur : l'écran de sélection de règles ne se fermait pas après avoir cliqué sur "Commencer la partie". Cause : `OnGUI` est appelé plusieurs fois par frame (passe Layout, l'événement de clic, puis Repaint) ; muter `selectedMode`/`MatchStarted` directement dans le handler d'un bouton change l'ensemble des contrôles dessinés lors d'une passe ULTÉRIEURE de la même frame par rapport à ce que la passe Layout avait déjà calculé — l'état interne de GUILayout se désynchronise et l'écran reste bloqué au lieu de basculer proprement. Fix dans `PoolMatchRules.cs` : les clics sur les boutons de mode et sur "Commencer la partie" posent maintenant des drapeaux `pendingModeChange`/`pendingStart`, appliqués dans `Update()` — la structure des contrôles reste identique sur toutes les passes d'une même frame, le changement ne prend effet qu'à partir de la frame suivante.
- Bug retour utilisateur (persistant après le fix précédent) : aucun bouton de l'écran de sélection ne réagissait vraiment au clic. Cause racine réelle : `FpsPlayerController`/`LocalFpsPlayerController` verrouillent et cachent le curseur (`Cursor.lockState = Locked`) dès que le joueur spawn — ce qui arrive avant même que l'écran de sélection des règles ne s'affiche, donc le curseur reste invisible et bloqué au centre de l'écran, hors d'atteinte de tous les boutons. Fix dans `PoolMatchRules.cs` : le curseur est explicitement déverrouillé/affiché en continu (`Update()`) tant que la partie n'a pas démarré (`!MatchStarted`), et reverrouillé/caché dès que `StartMatch()` s'exécute pour rendre la main au look FPS/visée normal.
- Note : les erreurs `TransformInspector`/`GameObjectInspector` (`NullReferenceException`, `SerializedObjectNotCreatableException`) rapportées par l'utilisateur restent inexpliquées mais semblent être du bruit Editor sans rapport avec le code du projet (aucun `Destroy` dans le code runtime, uniquement du `SetActive`) — probablement lié à un objet sélectionné dans la Hierarchy/Inspector au moment d'entrer/sortir du Play Mode. À surveiller si ça persiste après le fix du curseur.
- Demande utilisateur : prévoir des textures de bille pour pouvoir les distinguer (numéro/ordre), notamment pour le 9-ball et le 14.1 où l'ordre compte — jusqu'ici les billes n'avaient qu'une teinte plate, et 9-15 partageaient (par erreur) une couleur différente de leur pleine correspondante.
- Ajout de `Assets/Scripts/Editor/PoolBallTextureGenerator.cs` : génère une texture par bille (remplissage plein pour 1-7/8, bande blanche autour de l'équateur pour 9-15, badge circulaire blanc avec le numéro en police pixel 3×5 maison dessiné à deux endroits opposés), sauvegardée en PNG sous `Assets/Textures/Balls/` et réimportée comme asset. `PoolTableBuilder.CreateBall()` l'utilise maintenant comme `mainTexture` du matériau de la bille au lieu d'une simple teinte.
- Fix au passage : `BallColor()` dans `PoolTableBuilder.cs` calculait un mauvais index pour les billes rayées (9-15), les décalant d'un cran par rapport à leur pleine correspondante. Corrigé pour que 1↔9, 2↔10, ..., 7↔15 partagent la même couleur, comme un vrai jeu de billard.
- Retour utilisateur (capture d'écran) : l'ordre des billes dans le rack ne correspondait pas à la disposition standard. Cause : `RackBalls()` utilisait un compteur incrémental qui sautait son incrément à l'emplacement de la bille 8, ce qui le faisait repasser une seconde fois par la valeur 8 sur un autre emplacement (bille 8 en double, bille 15 jamais placée). Remplacé par un tableau `RackOrder` figé (1 à l'apex, 8 au centre, une pleine et une rayée aux deux coins arrière) reproduisant exactement la disposition de référence.
- Question utilisateur : le joueur 2 pouvait-il jouer hors tour en split-screen ? Réponse : oui, rien ne l'empêchait. Fix : `LocalPlayerHandController.TryPickUp()` refuse de ramasser la queue (elle seule) si ce n'est pas le tour de ce joueur (`PoolMatchRules.CanPlayerShoot()` comparé au `PlayerInput.playerIndex`), et `LocalPoolAimController` refuse aussi d'entrer en mode visée hors tour. Non appliqué côté online pour l'instant, faute d'identifiant de joueur stable réseau (`PoolMatchRules` n'est pas réseauté).
- Demande utilisateur : implémenter les pénalités (rater une bille, toucher la mauvaise en premier, empocher la bille blanche), avec une vraie main libre pour l'adversaire (option choisie face à un simple replacement automatique).
- `PoolBall.cs` : ajout d'un événement statique `CueBallFirstContact` (armé par `ArmContactTracking()`, appelée au moment du tir dans les deux `PoolAimController`, déclenché par `OnCollisionEnter` sur la première collision avec une autre bille) pour détecter le premier contact de la bille blanche pendant un tir. Ajout de `BeginBallInHand()`/`PlaceAt()`/`EndBallInHand()` (bille cinématique, collider désactivé, suit la position donnée) pour le placement en main libre.
- `IPoolRuleSet.ResolveShot()` reçoit maintenant ce premier contact. Chaque règle calcule sa propre légalité et appelle `match.RegisterFoul()` en plus du changement de tour habituel : 8-ball (mauvais groupe touché en premier, sauf avant attribution ou la 8 une fois le groupe nettoyé), 9-ball (pas la bille la plus basse encore sur la table — implémente enfin la vraie règle de contact, mise de côté lors de la première passe), 14.1 (seul "aucun contact" est fauté, pas de restriction de groupe).
- `PoolMatchRules.RegisterFoul()`/`ConfirmBallPlaced()`/`BallInHand` : gèrent la main libre. Le joueur qui récupère le tour se déplace normalement (queue pas nécessaire) et la bille blanche suit le point regardé sur le tapis (raycast caméra filtré via le nouveau marqueur `PoolTableSurface`, posé par `PoolTableBuilder.BuildSurface()` sur l'objet `Surface`, qui ignore les billes/bandes dans l'axe du regard et borne la position aux limites jouables). Validation avec Interact. Un scratch passe par le même chemin : `PoolBall.OnPocketed()` repositionne la bille blanche puis appelle directement `BeginBallInHand()` au lieu de juste relancer la physique.
- Nouveau `Assets/Scripts/Pool/PoolTableSurface.cs` (runtime, pas Editor-only).
- Limite connue : en ligne, la main libre fonctionne (chaque client détecte ses fautes localement) mais n'est pas restreinte au bon joueur, faute d'identifiant de joueur réseau fiable pour l'instant — à revoir avec la synchro réseau de la table.
- Retour utilisateur : une faute était déclenchée dès le lancement de la partie, sans qu'aucun tir n'ait eu lieu et sans que la bille blanche soit empochée. Cause : `PoolMatchRules` déduisait "un tir a eu lieu" uniquement du mouvement des billes (`AnyMoving()`) — le léger mouvement résiduel des billes qui se posent sous la gravité juste après le rack était donc pris pour un tir, et sa résolution (aucun contact enregistré) pour une faute instantanée. Fix : ajout de `PoolMatchRules.NotifyShotFired()`, appelée par les deux `PoolAimController` au moment réel du tir (`Shoot()`) — un tir n'est plus jamais déduit du seul mouvement des billes.
- Retour utilisateur : le placement de la bille en main libre (viser du regard en vue FPS classique) ne fonctionnait pas bien, suggestion de passer en vue de dessus. Remplacé le raycast caméra par une vraie vue plongeante pendant la main libre : `StartPlacementView()`/`EndPlacementView()` basculent la caméra au-dessus du centre de la table (droit vers le bas), désactivent le déplacement FPS, et la bille se déplace avec Move le long des axes propres de la table. `PoolTableSurface` expose maintenant une instance statique pour ce lookup.
- Retour utilisateur : la caméra de placement manquait de recul en split-screen. La hauteur était fixe, correcte en plein écran mais pas adaptée au ratio d'image réduit du split-screen. Remplacé par un calcul dynamique (`ComputePlacementHeight`) à partir du FOV/aspect réels de la caméra (qui reflète déjà la forme du viewport courant), avec une marge réglable (`Placement View Margin`). `PoolTableSurface` expose `HalfLength`/`HalfWidth` pour ce calcul.
- Retour utilisateur : une faute était déclenchée dès le lancement de la partie, sans qu'aucun tir n'ait eu lieu. Cause : `PoolMatchRules` déduisait "un tir a eu lieu" uniquement du mouvement des billes — le léger mouvement résiduel des billes qui se posent sous la gravité juste après le rack était pris pour un tir, et sa résolution (aucun contact) pour une faute instantanée. Fix : ajout de `PoolMatchRules.NotifyShotFired()`, appelée par les deux `PoolAimController` au moment réel du tir — un tir n'est plus jamais déduit du seul mouvement des billes.
- Retour utilisateur : un joueur solo (sans P2 en split-screen) se retrouvait totalement bloqué dès qu'une faute (ou un tour normal) passait la main à "joueur 2", faute de second contrôleur pour la reprendre. `CanPlayerShoot()` se rabat maintenant sur "toujours autorisé" quand au plus un `PlayerInput` local est actif (`PlayerInput.all.Count <= 1`) — un joueur seul peut jouer les deux tours ; un vrai split-screen à 2 joueurs garde l'application stricte du tour.
- Retour utilisateur : en split-screen, le stick droit du P2 semblait ne pas fonctionner, et la visée à la queue était très lente. Cause trouvée dans les prefabs : `PlayerLocal.prefab` et `PlayerOnline.prefab` avaient encore `gamepadSensitivity: 2`, l'ancienne valeur d'avant un fix précédent (jamais corrigée à la main sur les prefabs comme prévu à l'époque) — corrigé directement à 150 dans les deux fichiers `.prefab`. `PoolAimController`/`LocalPoolAimController` avaient aussi un seul `aimTurnSpeed` partagé souris/manette (même souci d'échelle que `FpsPlayerController` avant son fix) — scindé en `mouseAimTurnSpeed`/`gamepadAimTurnSpeed`, prefabs mis à jour en conséquence.
- Retour utilisateur : la vue de dessus pour la main libre restait trop proche même après le calcul dynamique par FOV/aspect. Cause : la marge (`Placement View Margin`) n'était que de 1.2 (20% de rab), un cadrage pile au bord de la table qui ne prend pas non plus en compte les bandes (qui débordent du tapis). Marge par défaut remontée à 1.6.
- Retour utilisateur (persistant) : "toujours trop proche" malgré la marge remontée, et demande explicite de pouvoir positionner soi-même la caméra. Cause réelle : le calcul automatique par FOV/aspect écrasait systématiquement la valeur du champ `Placement Camera Height` dès qu'une caméra était trouvée (ce qui est toujours le cas) — donc modifier ce champ dans l'Inspector n'avait aucun effet, contrairement à ce qu'on pourrait croire. Calcul automatique entièrement retiré : `Placement Camera Height` est redevenu la seule source de vérité (défaut remonté à 3.5), directement réglable dans l'Inspector des deux prefabs.
- Retour utilisateur : la ligne de visée disparaissait parfois selon la direction visée. Cause — bug Unity documenté : `Physics.SphereCast` ne rapporte jamais de collision contre un collider que la sphère recouvre déjà AU DÉBUT du balayage. Le cast partait pile au centre de la bille blanche avec son propre rayon, donc toute bille ou bande déjà au contact de la bille blanche au moment de viser était silencieusement ignorée par le cast. Fix (`PoolAimController.cs`/`LocalPoolAimController.cs`) : le cast démarre maintenant juste après la surface de la bille blanche (`ballPos + direction * (rayon + epsilon)`) au lieu de son centre.
- Retour utilisateur (persistant) : la ligne disparaissait encore par moments malgré ce fix. Plutôt que de continuer à traquer des cas particuliers un par un (viser à travers une poche, etc.), changement d'approche : la ligne de la bille blanche reste maintenant TOUJOURS affichée pendant la visée, étendue jusqu'à `Preview Max Distance` si rien n'est touché, au lieu de disparaître. Seul le segment de prédiction de la bille touchée (`objectBallPreview`) reste conditionnel à un vrai contact.
- Demande utilisateur : remplacer la table procédurale par un asset acheté (un seul mesh d'un bloc), avec la question de comment régler rebonds/friction dessus. Explication : un `Collider` ne porte qu'un seul `PhysicsMaterial` — impossible d'avoir bandes/tapis différents sur un unique MeshCollider global. Choix utilisateur : garder des colliders invisibles séparés (fidélité) plutôt qu'un compromis à un seul matériau.
- Ajout de deux nouvelles entrées de menu, **Tools > Pool > Attach Physics To Custom Table (Online/Offline)** : construisent exactement le même échafaudage physique (colliders de bandes/tapis avec les `PhysicsMaterial` déjà tunés, poches, rack, queues, `PoolMatchRules`) que les commandes existantes, mais sans les cubes visibles de bandes/tapis (`StripVisuals()` retire `MeshRenderer`/`MeshFilter`, ne garde que le `Collider`), et imbriqué comme enfant (`PoolPhysics`) sous l'objet actuellement sélectionné dans la Hierarchy (l'asset de table personnalisé) au lieu d'être autonome. `BuildTable()` accepte maintenant `hideVisuals`/`parentToSelection` (défaut `false`, comportement des commandes existantes inchangé). Les constantes en haut du fichier (`PlayLength`/`PlayWidth`/`TableSurfaceY`/etc.) restent la source de vérité des dimensions — à mesurer sur l'asset et ajuster avant de lancer la commande ; l'enfant généré part à la position locale (0,0,0), à recaler si le pivot de l'asset n'est pas à l'origine de la table.
- Demande de suivi : pouvoir régler les dimensions (`PlayLength`, `PlayWidth`, `TableSurfaceY`, etc.) directement dans l'éditeur plutôt que dans le code. Les anciennes `const float` en haut de `PoolTableBuilder.cs` sont devenues des champs sérialisés sur `PoolTableAssetSettings` (même asset que `customTablePrefab`, même menu **Tools > Pool > Select Custom Table Settings**) — visibles et modifiables dans l'Inspector, avec les mêmes valeurs par défaut qu'avant. `PoolTableBuilder` charge cet asset une fois en haut de `BuildTable()` (champ statique `settings`) et toutes les méthodes internes (`BuildSurface`, `BuildRails`, `CreateRail`, `BuildPockets`, `RackBalls`, `CreateBall`, `CreateCue`) lisent dessus désormais. Plus aucune modification de code nécessaire pour adapter les dimensions à un nouvel asset.
- Retour utilisateur (capture d'écran Hierarchy) : pas de doublon `PoolPhysics` (hypothèse écartée), position/rotation de la table correctes — mais les poches du milieu se retrouvaient en haut/bas au lieu d'être sur le côté, et le rack de billes mal orienté/mal placé. Cause : le code suppose toujours que l'axe X est le grand côté de la table (poches du milieu placées sur les longs côtés via Z) — si l'asset personnalisé a son grand côté le long de Z à la place, tout se construit tourné à 90° de la réalité. Fix tenté : détection automatique de l'axe le plus long via un guide (`bounds.size.x` vs `bounds.size.z`) + rotation appliquée en conséquence.
- Retour utilisateur (nouvelle capture) : toujours pas bon — les poches étaient enfin correctes, mais le reste (bandes, billes, queues) non, malgré la détection d'axe. L'utilisateur propose une approche différente : générer la physique avec des proportions cohérentes en interne, sans essayer de l'aligner automatiquement, et ajuster à la main. Décision : retrait complet de `MeasureFromPlayAreaGuide()`/du guide `PlayAreaGuide` — l'approche de mesure/alignement automatique s'est révélée trop fragile (pivot, échelle du plane, axe long) sur un asset arbitraire et difficile à déboguer à l'aveugle sans accès direct à l'éditeur. `Attach Physics To Custom Table` génère maintenant `PoolPhysics` à la position locale (0,0,0)/rotation identité, avec les dimensions de `PoolTableAssetSettings` telles quelles — l'utilisateur sélectionne ensuite cet objet entier et le repositionne/tourne (Scene view, gizmo normal) à l'œil pour l'aligner sur son asset, une seule manipulation visuelle directe au lieu de chasser des mesures automatiques.
- Demande utilisateur : démarrage du système de pouvoirs. Deux sources (billes spéciales empochées + caisses façon Mario Kart traversées par la bille blanche), pouvoir stocké (1 à la fois par joueur) et activable plus tard via une touche dédiée, plusieurs catégories prévues (Attaque/Piège/PowerUp/Destruction/Impact adverse). Portée de cette passe : le branchement complet + un pouvoir simple pour valider.
  - `PowerType.cs` (enum des catégories), `PoolPower.cs` (`ScriptableObject` abstrait, même pattern que `PoolPhysicsSettings`/`PoolTableAssetSettings` — un nouveau pouvoir = une nouvelle sous-classe + un asset), `BoostedShotPower.cs` (premier pouvoir concret, catégorie PowerUp : multiplie la puissance du prochain tir).
  - `PowerBall.cs` : marqueur sur une bille (même principe que `Cue.cs`), indépendant des règles de base — une bille à pouvoir compte normalement pour 8-ball/9-ball/14.1, en plus de donner le pouvoir.
  - `PoolPowerCrate.cs` : caisse posée sur le tapis, donne le pouvoir au joueur courant (`PoolMatchRules.CurrentPlayer`) quand la bille blanche passe assez près, se désactive après usage (pas de respawn pour l'instant).
- Retour utilisateur : la caméra de placement (vue de dessus après une faute) ne se repositionnait plus correctement. Cause — régression introduite par le shake caméra ajouté juste avant : `LocalPoolPowerEffectReceiver.LateUpdate()` ne connaissait que deux états (visée / normal FPS), pas ce troisième état de placement — qui pilote lui aussi `cameraTransform` directement en position/rotation monde chaque frame. Résultat : le code écrasait systématiquement cette position dès qu'il n'était pas en mode visée, MÊME sans pouvoir actif. Fix : nouvelle propriété `LocalPoolAimController.IsPlacementViewActive`, `LateUpdate()` ignore complètement la caméra tant que ce mode est actif.
- Demande utilisateur : rendre l'effet de Vision Impair plus percutant — clignotement plus rapide, et un léger shake caméra. Vérifié que le plugin "Feel" (More Mountains) mentionné par l'utilisateur n'est pas installé dans le projet — implémenté en code simple, sans dépendance, pour ne pas bloquer sur une installation.
  - Clignotement : remplacé le fondu progressif de l'overlay par un vrai flash on/off (`Mathf.Sin(Time.unscaledTime * flickerFrequency * ...)`), fréquence réglable (`Flicker Frequency`, défaut 8/s).
  - Shake caméra : ajout dans `LocalPoolPowerEffectReceiver.LateUpdate()` — délicat car la caméra est déjà pilotée directement par `LocalPoolAimController` pendant la visée (orbite autour de la bille, position/rotation monde recalculées chaque frame). Deux stratégies selon le contexte (nouvelle propriété `LocalPoolAimController.IsAiming`) : pendant la visée, décalage additif en espace MONDE par-dessus la position déjà calculée ce frame-là (sûr — l'orbite repart de zéro à chaque frame donc rien ne s'accumule) ; hors visée, reconstruction systématique depuis une position de repos locale connue (capturée une fois à `Awake()`) plutôt qu'un décalage additif, qui aurait dérivé indéfiniment puisque rien d'autre ne remet à zéro la position locale de la caméra en vue FPS normale. Exécuté en `LateUpdate()` (pas `Update()`) pour garantir de s'exécuter après le positionnement de la caméra ce frame-là, quel que soit le système qui l'a fait.
- Retour utilisateur : plus aucun effet en "mode vue solo" (hot-seat, un seul `PlayerInput` jouant les deux tours à tour de rôle) après le fix split-screen précédent. Cause : ce fix rendait le contrôle strict sur `playerInput.playerIndex` (toujours 0 avec un seul joueur physique), donc dès que `CurrentPlayer` passait à 1, plus rien ne correspondait — activation bloquée, et un effet ciblant le joueur 1 n'avait aucun récepteur pour le recevoir puisque le seul joueur physique reste toujours d'index 0. Fix : ajout de `PoolMatchRules.GetEffectivePlayerIndex(physicalPlayerIndex)` — renvoie l'index inchangé en vrai split-screen (2 `PlayerInput`), mais renvoie `CurrentPlayer` en hot-seat solo (1 seul `PlayerInput`, `PlayerInput.all.Count <= 1`), puisque le seul écran représente alors le camp dont c'est le tour plutôt qu'un joueur fixe. Appliqué aux trois points concernés : `LocalPoolPowerController` (activation), `LocalPoolAimController.EnterAim()` (démarrage du décompte), `LocalPoolPowerEffectReceiver` (sensibilité + overlay).
- Retour utilisateur : l'effet de Vision Impair s'appliquait sur les DEUX écrans en split-screen au lieu de seulement celui de l'adversaire ciblé. Cause : `OnGUI` dessine par défaut sur toute la fenêtre, pas sur le viewport du joueur concerné — l'overlay utilisait `new Rect(0, 0, Screen.width, Screen.height)`, couvrant les deux moitiés d'écran peu importe qui était ciblé. Fix : conversion du `Camera.rect` (viewport normalisé de CE joueur) en coordonnées écran (avec inversion de l'axe Y — l'espace viewport est bas-haut, l'espace GUI est haut-bas) pour restreindre l'overlay à sa seule moitié d'écran.
- Retour utilisateur : le décompte du pouvoir démarrait en temps réel dès l'activation, donc pouvait s'être dissipé avant même que l'adversaire n'ait son tour. Fix : `PoolMatchRules` sépare maintenant "pouvoir en attente" (`QueueVisionImpair`, posé à l'activation, timer PAS démarré) de "pouvoir actif" (`ConsumePendingVisionImpair`, qui démarre le vrai décompte) — le décompte ne démarre que lorsque le joueur ciblé entre effectivement en mode visée sur son propre tour, via un appel dans `LocalPoolAimController.EnterAim()` (déjà garanti n'arriver que sur son propre tour grâce à la vérification `CanShootNow()` existante).
- Demande utilisateur : nouveau pouvoir de catégorie Attaque, ciblant l'adversaire. Choix (parmi soi-buff sur le tir, perturbation visuelle/contrôles, ou autre) : perturbation de la vue/des contrôles FPS — plus immersif, mais nécessitait un nouveau canal pour qu'un pouvoir touche un joueur précis (rien de tel n'existait, tout passait jusqu'ici par "le joueur dont c'est le tour").
  - `PoolMatchRules.cs` étendu : suivi par joueur d'un trouble de vision temporisé (`ImpairVision`/`IsVisionImpaired`/`VisionImpairmentStrength`/`GetVisionImpairmentSensitivityMultiplier`), sur le même principe que `heldPower`/`shotPowerMultiplier` — l'état vit ici, le joueur concerné se contente d'interroger son propre statut chaque frame.
  - `VisionImpairPower.cs` (nouveau pouvoir, catégorie Attaque) : à l'activation, applique le trouble à l'ADVERSAIRE de celui qui l'active (`1 - activatingPlayer`) — écran qui blanchit puis s'estompe sur ~4s, sensibilité de visée réduite (×0.3 par défaut) pendant la même durée.
  - `LocalFpsPlayerController.cs` : nouveau `SensitivityMultiplier` (1 par défaut), multiplié dans `HandleLook()`. Pas ajouté côté online pour l'instant — inutilisable sans récepteur, aurait été un champ mort.
  - `LocalPoolPowerEffectReceiver.cs` (nouveau, offline uniquement) : interroge `PoolMatchRules` chaque frame pour appliquer le multiplicateur de sensibilité et dessiner l'overlay plein écran (`OnGUI`, alpha dépendant de `VisionImpairmentStrength`). Pas d'équivalent online pour l'instant — nécessite de savoir "je suis le joueur 0 ou 1", ce qui n'existe pas en ligne (même limite déjà acceptée pour la main libre et l'activation de pouvoir).
- Demande utilisateur : gérer le cas où la bille blanche saute par-dessus une bande et tombe hors de la table (rien ne le gérait, elle serait tombée indéfiniment). Ajout d'une détection dans `PoolBall.FixedUpdate()` : si une bille descend de plus de `Off Table Drop Threshold` (nouveau champ dans `PoolPhysicsSettings`, défaut 0.15m, confortablement plus grand qu'un simple rebond sur une bande) sous sa hauteur de tapis d'origine, elle est traitée exactement comme empochée — `OnPocketed()` réutilisé tel quel : pour la bille blanche, ça donne le même traitement qu'un scratch (repositionnement + main libre, comptabilisé comme faute par les règles existantes) ; pour les autres billes, retirée comme un empochage normal. Limite connue assumée : les vraies règles distinguent une bille numérotée qui saute de la table (une faute) d'un empochage légal — pas modélisé ici, à revoir si ça pose souci en pratique.
- Retours utilisateur (3 en une fois) :
  1. Le pouvoir de P1 se déclenche parfois quand P2 appuie sur sa touche pendant son propre tour. Cause : `LocalPoolPowerController` utilisait `CanPlayerShoot()`, qui a un repli "autoriser tout le monde" quand un seul `PlayerInput` local est actif (pensé pour ne pas bloquer un joueur solo testant seul) — ce repli n'a pas sa place pour l'activation d'un pouvoir, qui doit être strictement réservée au joueur à qui il appartient. Fix : vérification stricte `rules.CurrentPlayer == playerInput.playerIndex`, sans passer par `CanPlayerShoot()`.
  2. Le joueur pouvait ramasser une caisse (ou potentiellement empocher une bille à pouvoir) en déplaçant simplement la bille blanche pendant le placement en main libre. Fix : `PoolPowerCrate.FixedUpdate()` ignore maintenant le ramassage tant que `PoolMatchRules.BallInHand` est vrai. Vérifié que le cas des billes à pouvoir n'est en fait pas exploitable : le collider de la bille blanche est désactivé pendant toute la main libre (`BeginBallInHand()`), elle ne peut donc physiquement pousser aucune autre bille dans une poche pendant ce temps.
  3. La bille blanche ne rentre jamais dans les poches, même dès le premier scratch — pas une régression de code (confirmé : ça échoue systématiquement dès le premier essai, pas seulement après un événement particulier), mais la limite déjà documentée sur les tables personnalisées : les 6 poches générées (`Pocket_0`..`Pocket_5`) suivent une formule de rectangle idéal qui ne correspond pas forcément aux vrais trous visuels de l'asset. Ajout d'un Gizmo permanent (cercle rouge, visible même sans sélection) sur `PoolPocket.cs` pour visualiser où sont réellement les colliders invisibles et faciliter leur réalignement à la main sur les vrais trous.
- Retour utilisateur : le pouvoir semble s'appliquer sans appuyer sur la touche d'activation, de façon incohérente. Cause trouvée dans `Assets/InputSystem_Actions_Local.inputactions` (pas un bug de script) : l'utilisateur avait réassigné la touche d'activation ("Next") sur `<Keyboard>/a` — or cette touche est DÉJÀ liée à l'action "Move" (déplacement latéral gauche, la touche physique "Q" en AZERTY). Chaque appui sur la touche de déplacement gauche déclenchait donc aussi l'activation du pouvoir, sans lien apparent avec une vraie intention d'activation. Fix : rebinding sur `<Keyboard>/f` (libre) à la place de `<Keyboard>/a`. Vérifié que `InputSystem_Actions.inputactions` (online) n'a pas le même conflit.
- Retour utilisateur : le log de confirmation de ramassage empêchait de lire les autres logs — retiré (`PoolPowerCrate.cs`).
- Retour utilisateur : warning Unity répété en boucle "There are 2 audio listeners in the scene", noyant la console. Cause : `LocalFpsPlayerController.cs` (split-screen) ne gérait pas du tout l'`AudioListener`, contrairement à la version online qui le fait déjà (`audioListener.enabled = IsOwner`). En split-screen, contrairement à l'online, les deux caméras des joueurs doivent rester actives (chacune rend sa moitié d'écran) — mais un seul `AudioListener` doit rester actif à la fois. Fix : `Awake()` désactive maintenant l'`AudioListener` de tout joueur dont `PlayerInput.playerIndex != 0`, seul le joueur 1 garde le sien actif.
- Retour utilisateur : le warning persistait malgré le fix précédent. Cause supplémentaire trouvée directement dans `Assets/Scenes/SplitScreen.unity` — une "Main Camera" issue du template Unity par défaut, avec son propre `AudioListener` actif, indépendante des caméras des joueurs. Même avec un seul joueur actif, ça faisait toujours 2 (Main Camera + joueur 1). Fix : `AudioListener` de cette Main Camera désactivé directement dans le fichier de scène (`m_Enabled: 0`) — l'objet reste en place, juste son écouteur audio coupé.
- Retour utilisateur : doute sur la fiabilité du ramassage par la caisse. Cause potentielle réelle — `OnTriggerEnter`/`Collider` reste un test de recouvrement PAR PAS DE PHYSIQUE sous le capot ; les modes de collision "Continuous" protègent les collisions SOLIDES du tunneling, pas les triggers, donc une bille assez rapide pourrait en théorie traverser une fine zone de trigger entre deux pas sans jamais déclencher l'événement. `PoolPowerCrate.cs` réécrit : détection par vérification de distance explicite à chaque `FixedUpdate` (même limitation théorique en pratique, mais retire tout doute sur les prérequis Collider/Rigidbody des triggers Unity, et rend le rayon de ramassage trivial à visualiser/déboguer — Gizmo cercle cyan + log de confirmation à chaque ramassage). Ajout d'un visuel placeholder (cube) généré automatiquement s'il n'y a pas de `Renderer` sur l'objet, pour que la caisse soit visible sans configuration supplémentaire.
  - `PoolMatchRules.cs` étendu : `heldPower[]` (un pouvoir par joueur, écrase l'ancien si nouveau ramassage), `GrantPower()`/`TryActivatePower()`/`GetHeldPower()`, `SetShotPowerMultiplier()`/`ConsumeShotPowerMultiplier()` (consommé une seule fois puis remis à 1). `HandleBallPocketed()` vérifie maintenant aussi le marqueur `PowerBall`. Affichage du pouvoir tenu par chaque joueur ajouté à l'écran de statut.
  - `LocalPoolPowerController.cs`/`PoolPowerController.cs` (nouveaux, mêmes paires online/offline que le reste) : activent le pouvoir tenu sur pression d'une touche. Réutilisent l'action **"Next"** déjà présente dans les deux assets d'input (liée à la touche **2** au clavier et **D-pad droit** à la manette) mais jamais utilisée par aucun script du projet jusqu'ici — évite d'éditer les fichiers `.inputactions`, à renommer plus tard si besoin de clarté.
  - `PoolAimController.Shoot()`/`LocalPoolAimController.Shoot()` consomment maintenant le multiplicateur de puissance stocké avant de calculer l'impulsion du tir.
  - Limite connue (comme le reste des mécaniques de billard) : en ligne, pas d'identifiant de joueur fiable, donc `PoolPowerController` active/consomme pour `CurrentPlayer` sans vérifier que c'est bien CE client — même limitation déjà acceptée pour la main libre.
- Demande utilisateur : passe de relecture avant de démarrer les pouvoirs — vérifier ce qui est prêt/pas prêt pour le réseau, et chercher de la refacto. Réseau : `PoolBall`/`PoolMatchRules`/les `IPoolRuleSet`/`PoolTableSurface` restent tous network-agnostic (aucune dépendance `NetworkBehaviour`), donc rien à réécrire pour ça — les points d'attention déjà connus (simulation 100% locale par client, `CanPlayerShoot` basé sur `PlayerInput.playerIndex`) sont déjà documentés en commentaire/TODO, pas d'action requise maintenant. Refacto : ajout de `PoolBall.FindCueBall()` (boucle "trouver la bille blanche dans `Active`" qui était copiée-collée à l'identique dans `PoolMatchRules.RegisterFoul()` et les deux `HandleBallInHand()` des contrôleurs de visée) — les 3 call sites remplacés. Le reste de la duplication (`PoolAimController`/`LocalPoolAimController` quasi identiques) est laissé tel quel : c'est le choix d'architecture double codepath déjà validé avec l'utilisateur (voir mémoire projet), pas un défaut à corriger.
- Retour utilisateur : la bille blanche peut être placée en dehors de la zone de billard pendant la main libre. Bug réel dans `PoolTableSurface.ClampToPlayArea()` : elle utilisait `transform.InverseTransformPoint`/`TransformPoint`, qui divisent par l'échelle de l'objet `Surface` — or `Surface` a `localScale = (Play Length, épaisseur, Play Width)`, une échelle énorme et non uniforme, donc les coordonnées "locales" obtenues sont normalisées entre environ -0.5 et 0.5, pas en mètres réels, alors que `halfLength`/`halfWidth` SONT en mètres réels. Comparer une petite valeur normalisée à des bornes bien plus grandes en mètres faisait que le clamp ne se déclenchait quasiment jamais. Fix : remplacé par une rotation + translation manuelles (jamais de mise à l'échelle) pour obtenir un décalage en mètres réels depuis la position de `Surface`, peu importe son échelle.
- Retour utilisateur (suivi) : "pourquoi la bille rebondit parfois ?" — bug distinct des doublons, trouvé dans les matériaux physiques. `BallPhysics` avait `bounceCombine = Maximum`, or Unity résout le mode de combinaison d'un contact en prenant celui des deux matériaux qui a la priorité la plus haute (ordre : Average < Minimum < Multiply < Maximum) et l'applique aux DEUX valeurs de rebond, peu importe de quel côté il vient. Avec `Maximum` sur la bille, un contact bille/tapis passait donc AUSSI par `Maximum` (rien ne le bat), donnant `max(0.92, 0.05) = 0.92` — la bille rebondissait sur le tapis plat comme sur une autre bille, dès qu'elle allait assez vite pour dépasser le seuil de vitesse de rebond. Bug latent depuis le début, rendu plus visible par la table plus grande/tirs plus rapides. Fix : `BallPhysics.bounceCombine` passé à `Average` (priorité la plus basse, ne l'emporte jamais) — `RailPhysics` garde `Maximum` de son côté et suffit à lui seul à garantir des rebonds vifs sur les bandes, donc rien n'est perdu là ; bille/bille reste à 0.92 (Average des deux côtés) ; bille/tapis retombe correctement sur le `Minimum` du tapis (0.05).
- Retour utilisateur : une bille se retrouve parfois à "rebondir sur elle-même", et les touches ZQSD ne déplacent plus correctement la bille blanche en vue de dessus (main libre) une fois la table personnalisée tournée pour s'aligner sur l'asset.
  - Fix 1 (rebonds) : `Attach Physics To Custom Table` créait un nouveau `PoolPhysics` à chaque lancement sans jamais supprimer l'ancien — plusieurs relances (guide, puis valeurs corrigées) ont probablement empilé plusieurs `Surface`/bandes à des hauteurs légèrement différentes, coinçant la bille entre plusieurs colliders. `BuildTable()` supprime maintenant l'ancien `PoolPhysics` (si présent) avant d'en créer un nouveau.
  - Fix 2 (axes ZQSD) : le déplacement en main libre utilisait les axes propres de la table (`Surface.transform.right/forward`) — correct tant que la table n'était pas tournée, mais faux dès qu'on la tourne pour l'aligner sur un asset personnalisé (les axes de la table ne correspondent plus à ce qui est affiché à l'écran). Remplacé par les axes de la **caméra** (`cameraTransform.right`/`up`), toujours fixes en vue de dessus quelle que soit la rotation de la table — Move correspond maintenant toujours à ce qui est affiché à l'écran.
- Retour utilisateur : les billes paraissent déformées après ajustement. Cause — conseil erroné donné entre-temps ("redimensionne PoolPhysics non uniformément si besoin") : un `SphereCollider` (billes, poches) ne suit pas correctement une échelle non uniforme dans Unity, ce qui les étire en forme d'œuf. Correction du conseil (commentaire du menu + réponse utilisateur) : ne jamais toucher à l'Échelle de `PoolPhysics` (rester à 1,1,1) — ajuster plutôt directement `Play Length`/`Play Width`/`Table Surface Y` dans `PoolTableAssetSettings` (valeurs réelles en mètres) puis régénérer ; `PoolPhysics` ne sert qu'à Position/Rotation.
- Retour utilisateur : après génération sur l'asset personnalisé, rien n'était à la bonne place et les colliders bloquaient les trous des poches. Bug réel trouvé : `MeasureFromPlayAreaGuide()` mesurait bien la taille du guide, mais jamais sa position — le script laissait la racine `PoolPhysics` à la position locale (0,0,0), en supposant implicitement que le pivot du prefab était pile au centre de la table. Presque jamais vrai en pratique (pivot souvent à un coin, sur un pied, au sol), donc toute la physique générée (bandes, poches, rack) se retrouvait décalée par rapport à la vraie table, et les bandes tombaient au hasard par-dessus les vrais trous au lieu de laisser un passage. Fix : `MeasureFromPlayAreaGuide()` calcule maintenant aussi le centre XZ du guide en espace local (relatif au pivot de l'asset, via `InverseTransformPoint`) et le retourne ; `BuildTable()` positionne `PoolPhysics` à ce centre mesuré au lieu de `Vector3.zero`. `Table Surface Y` reste la hauteur du tapis relative à la racine (même sémantique qu'avant), donc aucune autre formule de positionnement n'a eu besoin de changer.
- Demande de suivi : pouvoir mesurer les dimensions sans calcul à la main, via un objet temporaire placé en enfant. Ajout de `MeasureFromPlayAreaGuide()` : si un enfant nommé exactement `PlayAreaGuide` (typiquement un `Plane` ajusté à l'œil sur le tapis) est trouvé sous l'asset de table au lancement de **Attach Physics To Custom Table**, ses bounds réels (`Renderer.bounds`) donnent directement `Play Length`/`Play Width` (taille X/Z) et `Table Surface Y` (haut des bounds) — écrits automatiquement sur `PoolTableAssetSettings`, puis l'objet guide est supprimé (temporaire, pas censé rester dans la table finale). Suppose que la table n'est pas tournée par rapport aux axes du monde (comme le reste du système déjà généré) ; sans guide trouvé, retombe sur les valeurs actuelles de l'asset de settings.
- Demande de suivi : pouvoir assigner un prefab que le générateur va chercher tout seul, plutôt que de devoir sélectionner l'asset à la main dans la Hierarchy avant de lancer la commande. Ajout de `Assets/Scripts/Editor/PoolTableAssetSettings.cs` (`ScriptableObject`, un seul champ `customTablePrefab`), auto-créé sous `Assets/Editor/PoolTableAssetSettings.asset` (même pattern que `PoolPhysicsSettings`), accessible via une nouvelle entrée **Tools > Pool > Select Custom Table Settings**. `Attach Physics To Custom Table` instancie maintenant ce prefab automatiquement (`PrefabUtility.InstantiatePrefab`, garde le lien prefab bleu, contrairement à `Object.Instantiate`) s'il est assigné ; sinon, retombe sur le comportement précédent (objet sélectionné dans la Hierarchy).
- Demande utilisateur : une queue devrait apparaître pour le 2e joueur aussi. Plutôt que de détecter dynamiquement l'arrivée d'un P2 (accroche sur `PlayerInputManager`, plus fragile), `PoolTableBuilder.BuildTable()` place directement **deux queues** dès la génération de la table (`Cue_P1`/`Cue_P2`, de part et d'autre de l'emplacement existant), qu'il y ait 1 ou 2 joueurs — chacun peut ramasser la sienne sans attendre que l'autre pose la sienne. Vaut pour les deux variantes (Online/Offline).
- Retour utilisateur : caméra de placement toujours trop proche en split-screen, mais correcte en solo. Cause : `Placement Camera Height` est une valeur manuelle réglée pour le plein écran — le split-screen rétrécit la largeur du viewport de chaque joueur (aspect plus étroit), ce qui réduit le champ de vision horizontal et fait paraître la même hauteur "trop proche" sur l'axe le plus long de la table. Fix : la hauteur manuelle sert maintenant de référence, mise à l'échelle automatiquement selon le ratio d'aspect réel du viewport courant par rapport à celui du plein écran (`PlacementHeightForCurrentViewport()`) — reste inchangée en solo (aspect identique), s'ajuste seule en split-screen.
- Retour utilisateur : une faute se déclenchait sur une casse pourtant légale, dès que le joueur empochait deux billes de groupes différents (une pleine + une rayée) au même coup. Debug via logs temporaires (`[PoolBall]`/`[PoolMatchRules]`) pour éviter de re-deviner à l'aveugle. Cause trouvée dans `EightBallRuleSet.ResolveShot()` : la variable `myGroup`, utilisée pour juger la légalité du premier contact, était réassignée EN PLEIN MILIEU de la boucle qui traite les billes empochées (dès qu'une bille du bon groupe tombe, le groupe du joueur se fixe) — donc si une bille de l'AUTRE groupe était traitée après coup dans la même boucle, la légalité du contact initial se retrouvait jugée contre le nouveau groupe fraîchement assigné au lieu du groupe (non assigné) d'avant le tir. Fix : capture de `groupAtShotStart` avant la boucle, utilisée pour la vérification de légalité (`myGroup` reste utilisé tel quel pour tout le reste : assignation de groupe, `groupCleared`, etc.). Même classe de bug corrigée dans `NineBallRuleSet.cs` (`nextBall` qui avançait avant d'être comparé au premier contact) via `targetBallAtShotStart`.

## 2026-08-25 (suite)
- Précision projet : split-screen local confirmé nécessaire en plus du multijoueur en ligne. Ajouté au backlog avec l'implication technique (plusieurs joueurs locaux = plusieurs objets réseau possédés par le même client, viewport + input séparés par joueur local) — pas implémenté maintenant, juste anticipé pour ne pas coder le futur système de visée billard d'une manière qui suppose un seul joueur local par écran.
- Début de la mécanique de billard : génération procédurale d'une table + rack complet de billes via un script éditeur (`Assets/Scripts/Editor/PoolTableBuilder.cs`, menu `Tools > Pool > Build Table`), plutôt que de la construire à la main dans l'éditeur — évite les erreurs de placement manuel et donne des dimensions cohérentes/ajustables par code. Ajout de `Assets/Scripts/Pool/PoolBall.cs` (frottement de roulement simulé) et `Assets/Scripts/Pool/PoolPocket.cs` (détection d'empochage). Pas encore réseauté — physique à valider en solo d'abord.
- Physique de la table validée fonctionnelle en solo par l'utilisateur.
- Décisions gameplay visée/tir : maintenir/relâcher pour charger et tirer (plutôt que tirer la souris en arrière façon 8 Ball Pool mobile) ; tir centré simple pour ce premier jet, effets/spin repoussés à une 2e passe.
- Ajout de `Assets/Scripts/Pool/PoolAimController.cs` sur le prefab joueur : détection de proximité de la bille blanche (marqueur `PoolBall.IsCueBall`), entrée en mode visée via l'action Interact (désactive `FpsPlayerController`, réutilise la même caméra du joueur en orbite autour de la bille via Look), charge/tir via Attack, prévisualisation optionnelle par `LineRenderer`. Ajout de `PoolBall.IsCueBall`/`Rigidbody` (accesseurs publics) et câblage du marqueur cue-ball dans `PoolTableBuilder.cs` via `SerializedObject`.
- Fix : détection de l'appui sur "Interact" peu fiable (interaction "Hold" configurée par défaut sur l'action) — combine maintenant `WasPressedThisFrame()` et `WasPerformedThisFrame()`.
- Fix : la ligne de prévisualisation de trajectoire restait invisible — elle se faisait masquer par le code qui cache le corps du joueur à sa propre caméra (`LineRenderer` hérite de `Renderer`). `FpsPlayerController.cs` exclut désormais `LineRenderer`/`TrailRenderer` de ce masquage.
- Fix : bille blanche non détectée par la proximité — la table en scène avait été générée avant l'ajout du marqueur `IsCueBall`, donc jamais cochée dessus. Corrigé manuellement sur l'instance existante (les tables régénérées via le menu l'auto-cochent désormais).
- Visée/tir de base validés fonctionnels par l'utilisateur : détection, entrée en mode visée, orbite caméra, charge/relâche pour tirer, prévisualisation.
- Demande utilisateur : physique propre pour les billes + matériaux adéquats sur les bandes. Réécriture de `PoolBall.cs` avec un vrai modèle glissement→roulement (friction au point de contact, couplage translation/rotation via le moment d'inertie d'une sphère pleine) au lieu du frein plat initial. Recalibrage des `PhysicsMaterial` (billes quasi élastiques entre elles, bandes rebondissantes réalistes) dans `PoolTableBuilder.cs`, avec correction du bug qui empêchait la mise à jour d'un PhysicsMaterial déjà existant lors d'une régénération. Réglages Rigidbody ajoutés (Continuous Dynamic pour éviter le tunneling à travers les bandes fines, Max Angular Velocity relevé, Interpolate). Puissance de tir recalibrée dans `PoolAimController.cs` — les anciennes valeurs donnaient des vitesses de bille irréalistes (>70 m/s) vu la masse réelle de 170g.
- Ajout du respawn de la bille blanche (`PoolBall.OnPocketed()`) : elle se replace à sa position de départ au lieu de se désactiver quand elle tombe dans une poche, pour pouvoir tester en continu sans avoir à la replacer à la main.
- Fix : caméra du mauvais côté en mode visée — `PoolAimController.cs` calculait la direction bille→joueur au lieu de joueur→bille pour l'angle de vue initial, plaçant la caméra à l'opposé d'où le joueur regardait.
- Fix : rebonds trop faibles / billes qui semblaient "coller" aux bandes — le réglage global Unity **Bounce Threshold** (`ProjectSettings/DynamicsManager.asset`) était resté à sa valeur par défaut (2 m/s), au-dessus de nos vitesses de bille désormais réalistes (0.5-6 m/s) : Unity désactive complètement le rebond en dessous de ce seuil. Baissé à 0.05 m/s. Baissé aussi `Default Contact Offset` (1cm → 2mm, bien trop grand pour des billes de 2.85cm de rayon). Bounciness des bandes montée à 0.85 avec `bounceCombine=Maximum` explicite des deux côtés (bille et bande) pour garantir un rebond franc sans dépendre des règles de priorité de combinaison entre matériaux.
- Fix : rebonds désormais permanents (les billes ne s'arrêtaient plus) — le Bounce Threshold à 0.05 était trop bas et re-déclenchait un rebond à la moindre micro-vitesse résiduelle au repos. Remonté à une valeur intermédiaire (0.3 m/s).
- Retour utilisateur : le modèle de friction en deux phases (glissement→roulement avec couplage angulaire complet) ne "sentait" pas naturel. Simplifié radicalement : `PoolBall.cs` réécrit avec une décélération proportionnelle/exponentielle de la vitesse (comme un vrai frottement de type "drag"), rotation visuelle simplifiée (calée directement sur la direction de déplacement, sans simulation physique du couple). Ajout de `Assets/Scripts/Pool/PoolPhysicsSettings.cs`, un `ScriptableObject` unique et partagé (chargé via `Resources.Load`, auto-créé par `PoolTableBuilder.cs` dans `Assets/Resources/`) contenant `frictionCoefficient`/`sleepVelocityThreshold` — modifiable directement dans son Inspector sans jamais avoir besoin de régénérer la table, contrairement aux anciens champs dupliqués sur chacune des 16 billes.
- Fix : `PoolPhysicsSettings` manquant dans `Assets/Resources` faisait planter `PoolBall.FixedUpdate()` en boucle (`NullReferenceException`) — donc plus aucun frottement ne s'appliquait du tout, d'où l'impression que la bille ne s'arrêtait jamais. `PoolBall.cs` retombe maintenant sur des valeurs par défaut en mémoire (avec un avertissement) si l'asset est introuvable, au lieu de planter.
- Retour utilisateur : la décroissance exponentielle avait le défaut inverse au seuil d'arrêt trop bas (traîne interminable à très basse vitesse) — remonter le seuil créait un arrêt trop brutal (à-coup visible). Ces deux symptômes sont une conséquence inhérente au modèle exponentiel. Remplacé par une **décélération constante** (type frottement réel) dans `PoolBall.cs` : la vitesse diminue d'une quantité fixe par seconde et atteint exactement zéro en un temps borné et prévisible (v/friction secondes), sans traîne ni à-coup. Champ renommé `frictionCoefficient` → `friction` dans `PoolPhysicsSettings.cs` pour refléter le changement d'unité (m/s² au lieu d'un taux de décroissance).
- Friction validée par l'utilisateur. Question soulevée : la bille blanche s'arrête net ("stun") sur un tir direct sans effet — confirmé comme un comportement physique correct (transfert total du mouvement entre deux billes de masse égale sans rotation avant), pas un bug. Laissé tel quel, se réglera avec les effets.
- Implémentation des effets/spin : `PoolAimController.cs` utilise maintenant l'action **Move** (libre pendant la visée) pour déplacer le point de frappe sur la bille blanche (X = effet latéral, Y = topspin/backspin), avec un indicateur visuel `OnGUI`. Le tir applique un couple (`AddTorque`, `ForceMode.Impulse`) calculé à partir du point de frappe, en plus de l'impulsion linéaire habituelle.
- Fix d'un conflit découvert en implémentant les effets : `PoolBall.cs` forçait la rotation à correspondre exactement au roulement à chaque frame, ce qui aurait immédiatement annulé tout effet appliqué au tir. Remplacé par une convergence progressive (`Lerp`) vers le roulement pur, à une vitesse réglable (`Spin Correction Rate`, nouveau champ dans `PoolPhysicsSettings.asset`) — l'effet persiste assez longtemps pour influencer les collisions suivantes (follow/draw) avant de s'estomper.
- Debug avec log temporaire : couple de torsion confirmé correctement calculé, mais aucun effet visible malgré ça. Cause racine trouvée : quand la bille blanche s'arrêtait net après un choc (vitesse linéaire ~0), le code remettait AUSSI la rotation à zéro immédiatement — détruisant l'effet avant qu'il ait pu se convertir en mouvement. Régression introduite lors de la simplification précédente du modèle de friction. Réintroduction du vrai couplage physique glissement/rotation (`PoolBall.cs`, deux méthodes séparées `ApplySlideFriction`/`ApplyRollingResistance`), tout en gardant la décélération constante validée par l'utilisateur. `Spin Correction Rate` retiré (obsolète), `Slide Friction` ajouté à `PoolPhysicsSettings.cs`.
- Deuxième bug trouvé juste après ("ça glisse sans arrêt") : la phase de roulement pur utilisait la même formule de couple que la phase de glissement, qui décélérait la rotation 2.5x trop vite par rapport à la vitesse linéaire — recréant sans cesse du glissement artificiel au lieu de se stabiliser. `ApplyRollingResistance` réécrite pour ralentir vitesse et rotation strictement ensemble (en maintenant v = ω × r), au lieu de passer par la formule de couple par impulsion (valide seulement pour le glissement).
- Log de diagnostic ajouté temporairement dans `PoolBall.cs` pour tracer vitesse/rotation/glissement en direct. Fausse piste initiale (doublon de bille), écartée par l'utilisateur (un seul tir, un seul objet). Données propres obtenues sur un rebond de bande : vitesse et glissement oscillaient en dents de scie entre deux valeurs sans jamais converger vers zéro.
- **Troisième bug, root cause réelle** : la limite de sécurité dans `ApplySlideFriction` (pour empêcher la correction de "dépasser" zéro) ne prenait en compte que l'effet direct sur la vitesse linéaire, pas l'effet couplé sur la rotation qui amplifie l'impact réel sur le glissement d'un facteur **3.5** (constante physique connue pour une sphère pleine : 1 + 1/0.4). Résultat : dès que le glissement devenait petit, la correction le faisait rebondir de l'autre côté au lieu de converger — explique tous les symptômes "ça glisse sans arrêt" observés. Clamp corrigé pour diviser par ce facteur.
- Trois nouvelles demandes : prédiction de trajectoire (avec masquage au-delà d'une distance), interdiction de viser tant que la bille blanche roule, queue de billard visuelle. Implémentées dans `PoolAimController.cs` : blocage de la cible via la vitesse de la bille dans `FindNearbyCueBall()` ; prédiction de la bille touchée via `SphereCast` + physique de collision élastique (axe des centres au contact), masquage complet si rien n'est touché dans la distance max ; queue placeholder générée par script (cylindre), positionnée dans l'axe de visée avec recul visuel pendant la charge. Champ `Trajectory Preview` renommé `Cue Ball Preview`, nouveau champ `Object Ball Preview` ajouté.
- Demande de suivi : faire de la queue un vrai objet ramassable/posable, avec un vrai système générique de préhension d'objets plutôt qu'une version limitée à la canne (choix de l'utilisateur). Ajout de `Assets/Scripts/Interaction/Grabbable.cs` (ramassage/dépôt réseauté, autoritaire serveur via ServerRpc, parentage NGO automatique pendant le port), `Cue.cs` (marqueur), `PlayerHandController.cs` (détection/ramassage/dépôt côté joueur, coordination avec `PoolAimController` pour le partage de la touche Interact). `PoolAimController.cs` retravaillé : la visée exige maintenant de tenir la queue ; le placeholder auto-généré au runtime est remplacé par le vrai objet tenu, repositionné dynamiquement pendant la visée. `PoolTableBuilder.cs` génère un objet Cue physique ramassable près de la table.
- Fix : Interact près du billard mais pas assez près de la bille blanche précisément faisait déposer la queue au lieu de viser (`Interact Range` trop court, 2m, par rapport à la taille de la table 2.24m). Remonté à 3.5m par défaut — compromis, pas une solution définitive tant que viser et déposer partagent la même touche.
- Fix critique : la queue gardait son collider actif une fois en main, donc en la téléportant près du joueur (potentiellement à travers la table/les billes) elle les poussait comme un objet solide. `Grabbable.cs` désactive maintenant le collider (et le kinematic) via un callback `NetworkVariable.OnValueChanged` qui s'exécute chez tous les clients (pas juste sur le serveur, où tournait le `ServerRpc`) — bug qui aurait aussi causé des désyncs de physique côté client non-hôte.
- Fix : la queue traversait le sol à la dépose. Deux protections ajoutées : léger décalage vers le haut avant de réactiver la physique (le collider étant resté désactivé pendant le port, l'endroit où elle se trouve peut être imbriqué dans une géométrie solide), et détection de collision continue sur son Rigidbody (`PoolTableBuilder.cs`) pour éviter le tunneling à travers le sol.
- Fix : la queue se positionnait par son CENTRE (pas sa pointe) sur le point de contact avec la bille blanche pendant la visée — la moitié de la queue traversait donc la bille. `UpdateCueVisual()` corrigé pour décaler le pivot de la moitié de la longueur réelle de la queue (dérivée de son échelle) le long de l'axe de visée.
- Démarrage du split-screen local (2 joueurs, décision : jamais réseauté, contrairement au reste du jeu qui passe toujours par Netcode même en solo). Plutôt que de risquer de casser les contrôleurs réseau (beaucoup de mise au point dedans), création d'un second jeu de scripts parallèle sans Netcode : `LocalFpsPlayerController.cs`, `LocalGrabbable.cs`, `LocalPlayerHandController.cs`, `LocalPoolAimController.cs` — même logique de jeu, actions lues via `PlayerInput` (un par joueur) au lieu d'un asset partagé. `Cue.cs` rendu générique pour marquer aussi bien un `Grabbable` qu'un `LocalGrabbable`. Appairage clavier/manette et découpage d'écran délégués à `PlayerInputManager` d'Unity. `PoolTableBuilder.cs` : menu renommé en "Build Table (Online)", nouveau "Build Table (Offline / Split-Screen)" ajouté pour générer une queue non-réseautée.
- Ajout d'un cheat code de test : `Assets/Scripts/Core/SplitScreenCheatSpawner.cs` — maintenir C+P force l'arrivée d'un 2e joueur local via `PlayerInputManager.JoinPlayer()` (appairé à une manette connectée si disponible), pour tester le split-screen sans avoir besoin d'appuyer sur un bouton d'un second appareil physique.
- Essai puis abandon d'un asset tiers Asset Store, "Adaptive Split Screen" (`com.projectdawn.splitscreen`) : après lecture de son code source, incompatibilité de fond confirmée — il force toutes les caméras des joueurs à partager la même rotation (conçu pour de la vue de dessus/troisième personne façon leur démo "Tanks", pas pour un FPS où chaque joueur regarde librement). Retiré, retour à `PlayerInputManager` seul.
- Fix : `PlayerInput` avertissait que `InputSystem_Actions.inputactions` est enregistré comme asset "project-wide actions" (singleton partagé dans tout le projet), déconseillé par Unity avec `PlayerInput` — risque de mélange d'input entre les deux joueurs en split-screen. Créé `Assets/InputSystem_Actions_Local.inputactions`, copie indépendante (mêmes bindings, juste la map "Player") non enregistrée comme project-wide, dédiée à `PlayerInput`/split-screen.
- Split-screen fonctionnel confirmé par l'utilisateur. Retour : sensibilité du stick manette trop élevée en vue FPS, et plus faible en mode visée billard. Root cause : `HandleLook()` (`FpsPlayerController.cs` ET `LocalFpsPlayerController.cs`) n'appliquait jamais `Time.deltaTime` — correct pour la souris (le delta est déjà "par frame"), mais faux pour un stick de manette (qui donne une valeur continue, à multiplier par le temps écoulé pour donner une vitesse de rotation stable indépendante du framerate). Le mode visée billard, lui, le faisait déjà correctement, d'où la différence de ressenti. Fix appliqué aux deux contrôleurs FPS ; `Gamepad Sensitivity` par défaut remonté de 2 à 150 dans le code (l'ancienne valeur, pensée pour la formule non-multipliée, serait maintenant beaucoup trop faible) — à corriger manuellement sur les prefabs existants (**Player** en ligne et **LocalPlayer** hors-ligne), même souci de valeur déjà enregistrée que d'habitude.

## 2026-09-01
- Demande utilisateur : la durée de l'effet de `VisionImpairPower` doit durer tout le temps où le joueur affecté tient la queue et jusqu'à la fin de son tour, plutôt qu'une durée fixe en temps réel. `PoolMatchRules.cs` : remplacement du modèle basé sur un timer (`visionImpairedUntil`/`ImpairVision(player, duration, sensitivityMultiplier)`, valeur d'intensité qui s'estompait avec le temps) par un modèle booléen lié au tour (`visionImpaired[]`, `QueueVisionImpair(player, sensitivityMultiplier)` sans paramètre de durée, `VisionImpairmentStrength` renvoie 1 ou 0 sans fondu). `SwitchTurn()` efface maintenant `visionImpaired[CurrentPlayer]` avant de faire passer la main — c'est ce qui termine effectivement l'effet à la fin du tour du joueur affecté. `VisionImpairPower.cs` : champ `duration` retiré, `Activate()` appelle `QueueVisionImpair` avec la nouvelle signature à 2 paramètres. Le déclenchement reste inchangé (toujours différé jusqu'à l'entrée en mode visée du joueur ciblé via `ConsumePendingVisionImpair`, voir `LocalPoolAimController.EnterAim()`), seule la fin de l'effet change.
- Retour utilisateur : l'effet devait aussi s'arrêter dès que le joueur affecté SORT du mode visée, pas seulement à la fin de son tour (sinon le clignotement/shake continuait pendant qu'il se déplaçait entre deux visées). Ajout de `PoolMatchRules.EndVisionImpair(player)`, appelé depuis `LocalPoolAimController.ExitAim()` (symétrique de `ConsumePendingVisionImpair()` dans `EnterAim()`). `SwitchTurn()` garde son propre nettoyage de `visionImpaired[CurrentPlayer]` en filet de sécurité, pour le cas où le tour se termine sans passage explicite par `ExitAim()`.
- Ajout en TODO (pas encore implémenté) : la logique de déclenchement des pouvoirs (`LocalPoolPowerController`/`PoolPowerController`) bloque actuellement toute activation hors du tour du joueur, ce qui est trop restrictif pour les pouvoirs qui visent l'adversaire (Attaque, futur Piège) — un joueur devrait pouvoir les activer même quand ce n'est pas son tour. Les pouvoirs qui influent directement sur sa propre bille/façon de jouer (ex. `BoostedShotPower`) doivent en revanche rester restreints à son propre tour.
- Utilisateur a installé le plugin **Feel** (MoreMountains) — confirmé présent dans `Assets/Plugins/Feel` (`MMFeedbacks`, `MMTools`, `NiceVibrations`). Discussion sur son usage : plutôt qu'un remplacement immédiat du shake/flash manuel du Vision Impair (déjà validé fonctionnel, laissé tel quel), décision de faire une passe de game feel plus large plus tard. Ajout en TODO d'une entrée dédiée listant les moments déjà accrochables (tir, bille empochée, faute, ramassage de pouvoir) et la piste technique pour le shake caméra par joueur en split-screen (`MMCameraShaker` par caméra avec channel = `playerIndex`, pour éviter qu'un shake fuite sur l'écran de l'autre joueur).
- Ajout en TODO : système de combo sur les billes empochées enchaînées, avec multiplicateur et rendu "ultra juicy" (texte, shake/flash qui montent en intensité) — à concevoir une fois la passe de game feel amorcée. Portée exacte du multiplicateur (score, effet, cosmétique) et condition de reset pas encore décidées.
- Décision : avant d'attaquer la passe de juice, initialisation d'un dépôt **git** à la racine du projet (absent jusqu'ici) — demande explicite de l'utilisateur de pouvoir revert si besoin. `.gitignore` Unity standard ajouté (exclut `Library/`, `Temp/`, `Logs/`, `UserSettings/`, les fichiers de projet IDE générés `*.csproj`/`*.sln`/`*.slnx`), commit initial `6c12b34` posé comme point de retour (`Assets/`, `ProjectSettings/`, `Packages/`, `TODO.md`, `CHANGELOG.md`, `.gitignore`).
- Première passe de juice implémentée (offline/split-screen uniquement, même limitation que le Vision Impair — nécessite `PlayerInput.playerIndex`). `LocalPoolPowerEffectReceiver.cs` étendu avec un système générique de shake caméra + flash écran à impact, à durée fixe et décroissance linéaire (`RequestShake`/`RequestFlash`), combiné additivement avec le shake du Vision Impair (au lieu de l'écraser) dans le même `LateUpdate` déjà en place — pas un second composant séparé, pour éviter d'avoir un deuxième "écrivain" indépendant sur la transform caméra (classe de bug déjà rencontrée deux fois cette session). Branchements :
  - **Tir** : `LocalPoolAimController.Shoot()` appelle `PlayShotFeedback()` sur le récepteur du tireur, juste après `ExitAim()` (la caméra est déjà repassée en contrôle FPS local-position normal à ce moment, donc pas de conflit avec l'orbite de visée).
  - **Bille empochée** : `PoolBall.Pocketed` (event statique déjà existant) — ressenti sur les deux écrans, pas de ciblage par joueur (moment partagé de la table).
  - **Faute** : nouvel event statique `PoolMatchRules.Fouled`, déclenché dans `RegisterFoul()` avant `SwitchTurn()` — ressenti sur les deux écrans, flash rouge distinct du flash doré des empochages.
  - **Ramassage de pouvoir** : nouvel event statique `PoolMatchRules.PowerGranted(int player)`, déclenché dans `GrantPower()` (donc couvre à la fois les caisses `PoolPowerCrate` et les billes `PowerBall`) — ciblé uniquement sur le joueur concerné via `GetEffectivePlayerIndex`, pas de fuite vers l'autre écran.
  - Décrémentation des timers de shake/flash à impact faite dans `Update()` (une fois par frame), jamais dans `OnGUI()` — leçon déjà apprise cette session avec le bug de l'écran de sélection de mode (`OnGUI` peut s'exécuter plusieurs fois par frame, Layout puis Repaint).
  - **Décision technique** : pas d'utilisation de `MMCameraShaker`/`MMWiggle` de Feel pour ces shakes malgré le plugin maintenant installé — la caméra joueur a déjà 3 systèmes qui écrivent dessus selon l'état (FPS normal, orbite de visée, vue de placement bille-en-main) ; un `MMWiggle` indépendant se ferait notamment écraser par la remise au repos du `LateUpdate` existant. Réutilisation de la plomberie caméra maison déjà validée à la place. Le pop visuel Feel sur l'objet caisse au ramassage (candidat plus sûr, objet isolé) reste à faire, noté en TODO.
- Retour utilisateur sur le tir : demande d'un zoom caméra progressif vers le bout de la queue pendant la charge (comme une jauge qui se charge), avec un shake léger qui grandit avec la charge, et un effet lumineux plus "explosif" au relâché.
  - `LocalPoolAimController.cs` : nouveau champ `chargeZoomDistance` et propriété publique `ChargeFraction` (0 à 1, `chargedPower / maxPower`) ; `UpdateAim()` réduit `orbitDistance` proportionnellement à `ChargeFraction` avant de positionner la caméra — la caméra orbite donc de plus en plus près à mesure que la jauge se charge, sans toucher au reste du calcul (LookAt, preview, visuel de la queue).
  - `LocalPoolPowerEffectReceiver.cs` : nouveau champ `chargeShakeMaxMagnitude`, combiné dans `LateUpdate()` avec les autres sources de shake déjà additives (Vision Impair + impact à durée fixe) — contrairement à celles-ci, ce n'est pas une impulsion à décroissance mais une magnitude qui suit `ChargeFraction` en direct, donc nulle dès que le joueur relâche/arrête de charger. `PlayShotFeedback()` (déjà appelée par `Shoot()`) enrichie avec un flash vif et chaud (`shotFlashColor`/`shotFlashMaxAlpha`/`shotFlashDuration`) en plus du kick de recul déjà existant, pour lire comme un petit éclat de lumière au moment de l'impact.
  - Pas de nouveau composant : le zoom reste dans `LocalPoolAimController` (seul propriétaire légitime de la position caméra en visée) et le shake/flash restent dans `LocalPoolPowerEffectReceiver` — même séparation des responsabilités que le reste de cette passe.
- Retour utilisateur sur la bille empochée : demande d'un halo lumineux bref sortant du pocket où la bille a été empochée (plutôt qu'un flash plein écran), en gardant un léger shake. `PoolPocket.cs` (déjà l'endroit qui sait exactement où/quand une bille tombe, via `OnTriggerEnter`) : ajout d'une `Light` point réutilisée (auto-créée en enfant si absente, même pattern que le visuel placeholder de `PoolPowerCrate`), déclenchée à chaque `OnTriggerEnter` via une coroutine qui fait décroître son intensité linéairement de `haloIntensity` à 0 sur `haloDuration`, puis se désactive. `LocalPoolPowerEffectReceiver.HandleBallPocketed` ne fait plus de `RequestFlash` (champs `potFlashColor`/`potFlashMaxAlpha`/`potFlashDuration` retirés, devenus inutilisés) — garde uniquement `RequestShake`. Choix d'implémentation : directement dans `PoolPocket` plutôt qu'un event séparé + listener, puisque c'est déjà le seul endroit qui connaît la position ET le moment exacts, pas besoin de faire voyager l'info ailleurs. Pas de dépendance à un asset externe (texture/particule) — juste le component `Light` standard, compatible URP nativement. Pas encore calibré en jeu — l'intensité dépend de si **Physical Light Units** est activé dans les réglages URP du projet.
- Retour utilisateur (avec référence visuelle) : le halo doit en plus s'élever, façon prefab **CFXR3 Magic Aura**. Vérification faite que le pack **Cartoon FX Remaster** (JMO Assets) est déjà installé dans le projet (`Assets/Plugins/JMO Assets/Cartoon FX Remaster`) et contient bien `CFXR Prefabs/Magic Misc/CFXR3 Magic Aura A (Runic).prefab` — utilisé tel quel. `PoolPocket.PlayHalo()` instancie ce prefab à la position du pocket en plus d'animer la `Light` existante ; comme ses systèmes de particules ont `Stop Action = None` (vérifié dans le prefab), l'instance ne se nettoie pas toute seule — `Destroy(aura, risingAuraLifetime)` s'en charge après un délai réglable (2s par défaut). `PoolTableBuilder.BuildPockets()` charge le prefab une fois via `AssetDatabase.LoadAssetAtPath` et l'assigne automatiquement sur chaque pocket généré (`SerializedObject`, même pattern que le reste du builder) — mais les tables déjà construites avant ce changement doivent être régénérées ou avoir le champ assigné à la main.
- Retrait du flash blanc plein écran au tir (`PlayShotFeedback()` ne fait plus que le kick de recul) — retour utilisateur que ça ne rendait pas bien. Champs `shotFlashColor`/`shotFlashMaxAlpha`/`shotFlashDuration` retirés de `LocalPoolPowerEffectReceiver`, devenus inutilisés.
- Retour utilisateur : le halo CFXR3 Magic Aura est beaucoup trop gros et trop lent (échelle/rythme pensés pour un tout autre usage dans le pack d'origine). `PoolPocket.PlayHalo()` ajuste maintenant l'instance générée sans toucher au prefab partagé : `risingAuraScale` (0.4 par défaut) réduit `transform.localScale`, `risingAuraSpeedMultiplier` (1.8 par défaut) multiplie `simulationSpeed` sur chaque `ParticleSystem` de la hiérarchie instanciée — fonctionne même appliqué juste après `Instantiate()` (Play On Awake a déjà démarré la simulation la même frame), car `simulationSpeed` est relu à chaque frame plutôt que figé au lancement.
- Retour utilisateur : l'arrêt du halo (l'aura montante) est trop brutal, faudrait un léger fondu ; et pourquoi pas un petit ralenti au moment où une bille est empochée.
  - `PoolPocket.cs` : remplacement du `Destroy(aura, risingAuraLifetime)` immédiat par une coroutine `AuraWindDownRoutine` qui appelle `ParticleSystem.Stop(true, ParticleSystemStopBehavior.StopEmitting)` sur chaque système de particules après `risingAuraActiveDuration` (arrête juste l'émission de nouvelles particules) puis ne détruit l'objet qu'après un délai `risingAuraFadeOutBuffer` en plus, laissant les particules déjà en vol finir leur propre fondu au lieu de disparaître d'un coup — c'était ça le côté "brutal", pas la lumière. En profite pour adoucir aussi le fondu de la `Light` du halo (`HaloRoutine`) : passage d'une décroissance linéaire à une décroissance quadratique (ease-out), plus douce en fin de course.
  - Ajout d'un petit ralenti (hitstop) à chaque bille empochée : `PoolMatchRules.cs`, nouveaux champs `potSlowMotionScale` (0.3 par défaut) et `potSlowMotionDuration` (0.12s réel, `WaitForSecondsRealtime` — la durée du dip elle-même ne doit pas dépendre du ralenti qu'elle crée). Posé dans `HandleBallPocketed` (déjà l'unique abonné global à `PoolBall.Pocketed`), pas dans `PoolPocket` ni le receveur par joueur : `Time.timeScale` est une valeur globale unique, et 6 pockets ayant chacun leur propre coroutine indépendante se seraient fait concurrence (l'une remettant `timeScale` à 1 pendant qu'une autre pense encore être en train de ralentir). Un nouveau pot pendant un ralenti déjà en cours relance le dip depuis le début plutôt que d'empiler. Filet de sécurité dans `OnDestroy()` : `Time.timeScale = 1f` pour ne jamais laisser le jeu bloqué au ralenti si `PoolMatchRules` est détruit en plein dip. Aucun changement nécessaire ailleurs dans le système de juice : les timers de decay du shake/flash à impact utilisaient déjà `Time.unscaledDeltaTime`, donc ils ne s'étirent pas artificiellement pendant le ralenti ; la lumière du halo et l'aura CFXR, elles, utilisent le temps normal et ralentissent donc naturellement avec le reste du monde — cohérent avec l'effet recherché.
- Ajout en TODO : centraliser les paramètres actuellement éparpillés en `[SerializeField]` par composant (pouvoirs, juice caméra/shake/flash, etc.) dans des fichiers de config `ScriptableObject` dédiés et partagés, sur le modèle déjà établi par `PoolPhysicsSettings`/`PoolTableAssetSettings`.
- Implémentation de l'item TODO ci-dessus, pour le juice caméra/pot (les pouvoirs suivaient déjà ce pattern individuellement — un `ScriptableObject` par pouvoir, `BoostedShotPower`/`VisionImpairPower` — donc rien à faire de ce côté). Deux nouveaux assets, même pattern que `PoolPhysicsSettings` (Resources-loaded, auto-créés, valeurs par défaut copiées à l'identique des anciens champs pour ne rien changer visuellement) :
  - `PoolScreenJuiceSettings.cs` : tout ce que possédait `LocalPoolPowerEffectReceiver` (overlay/shake Vision Impair, shake de charge, shake tir/pot/faute/ramassage, flash faute/ramassage) **plus** `chargeZoomDistance`, qui vivait sur `LocalPoolAimController` — regroupés dans le même asset parce que c'est le même moment ressenti ("charger un tir") même s'ils sont lus par deux scripts différents. `LocalPoolPowerEffectReceiver` et `LocalPoolAimController` chargent chacun leur propre référence statique via `Resources.Load` en `Awake()` (même fallback silencieux + `Debug.LogWarning` que `PoolBall`), pas de cache partagé entre les deux — cohérent avec le fait que `Resources.Load` est déjà bon marché et que `PoolBall` fait pareil.
  - `PoolPotEffectSettings.cs` : tout ce que possédait `PoolPocket` (couleur/intensité/portée/durée du halo, prefab et réglages de l'aura montante) **plus** `potSlowMotionScale`/`potSlowMotionDuration`, qui vivaient sur `PoolMatchRules` — même raisonnement, "ce qui se passe quand une bille tombe" regroupé au même endroit. Bénéfice concret ici plus net que pour l'asset caméra : contrairement aux prefabs joueur (une seule source, dupliquée par instanciation), les 6 `PoolPocket` sont 6 objets générés indépendamment par `PoolTableBuilder` — avant cet asset, ajuster le halo demandait de modifier les 6 Inspectors un par un.
  - `PoolTableBuilder.cs` : `EnsureScreenJuiceSettingsAsset()`/`EnsurePotEffectSettingsAsset()` ajoutés à côté de `EnsurePhysicsSettingsAsset()` (même règle : ne jamais écraser un asset déjà présent), `EnsurePotEffectSettingsAsset()` assigne aussi le prefab CFXR3 Magic Aura à la création. Le wiring par-pocket de `risingAuraPrefab` via `SerializedObject` dans `BuildPockets()` (ajouté la fois précédente) est retiré — devenu inutile, `PoolPocket` n'a plus ce champ.
  - Retour utilisateur après test : le fade out de l'aura ne fonctionnait pas vraiment — "ça laisse le FX le temps indiqué puis ça s'arrête brutalement". Root cause : `AuraWindDownRoutine` devinait un délai fixe (`risingAuraFadeOutBuffer`, 1s) avant de `Destroy()` l'instance, sans jamais vérifier si les particules avaient réellement fini leur vie — dès que leur durée de vie réelle dépassait cette seconde devinée, l'objet était détruit en plein fondu, donnant l'impression d'une coupure nette. Remplacé par un sondage de `ParticleSystem.IsAlive()` (l'API prévue pour ça) sur chaque système de la hiérarchie, frame par frame, après l'arrêt de l'émission — l'objet n'est détruit que lorsque plus aucune particule n'est réellement en vie. `risingAuraFadeOutBuffer` change de sens : ce n'est plus un délai fixe mais un plafond de sécurité (relevé à 4s) pour le cas où un système ne rapporterait jamais "mort" (ex. un système en boucle).
  - En creusant ce bug, découverte que `Assets/Plugins` a été réorganisé côté utilisateur depuis la dernière session (`JMO Assets` déplacé vers `Assets/Plugins/VFX/JMO Assets`, `Feel` vers `Assets/Plugins/OverAll/Feel`) — le chemin en dur vers le prefab CFXR3 Magic Aura dans `PoolTableBuilder.EnsurePotEffectSettingsAsset()` était devenu obsolète, corrigé en conséquence (sans impact sur l'asset déjà créé, dont la référence survit aux déplacements de dossier grâce au GUID).
  - **Nouveau menu dédié, non-destructif** : `Tools/Pool/Ensure Config Assets Exist` — appelle uniquement les trois `Ensure*Asset()`, sans toucher au reste. Ajouté exprès en dehors de `BuildTable()`/"Attach Physics To Custom Table" : ces derniers détruisent et régénèrent tout le rig "PoolPhysics" à chaque exécution (constaté en relisant le code), pockets compris — les re-lancer juste pour faire naître les 2 nouveaux assets aurait écrasé l'alignement manuel déjà fait sur les 6 pockets de la table en cours. Au passage, le message d'avertissement de repli de `PoolPhysicsSettings` (`PoolBall.cs`) pointait vers "Build Table", pour la même raison risqué à suggérer une fois qu'on sait ça — corrigé pour pointer vers ce nouveau menu sûr, comme les 4 nouveaux messages des configs juice/pot.
- Ajout de deux entrées TODO demandées par l'utilisateur, sans code associé pour l'instant :
  - Revoir la condition de victoire en 8-ball. Vérification faite en relisant `EightBallRuleSet.ResolveShot` : "8 + blanche dans le même tir = défaite" et "8 avant d'avoir nettoyé son groupe = défaite" sont **déjà** gérés correctement (`foul` inclut `cueBallPocketed`, victoire conditionnée à `groupCleared && !foul`). Ce qui manque : annonce de poche pour la bille 8 spécifiquement (exception fréquente aux règles casual même sans call-shot général, à trancher), et un affichage de victoire/défaite plus lisible qu'`OnGUI` basique.
  - Scinder les modes de jeu vis-à-vis des pouvoirs : `PoolPowerCrate`/`PowerBall` ne regardent actuellement pas `PoolMatchRules.selectedMode` (privé, pas de getter public) — les pouvoirs sont donc ramassables/activables même en 8-ball/9-ball/14.1 "classique", alors qu'ils étaient pensés réservés au mode Party.
- Utilisateur a listé un ensemble de pouvoirs candidats pour discussion (lequel implémenter en premier) — ajoutés en TODO sous forme de backlog, aucun choisi/implémenté : Aimant (attire la bille vers le pocket le plus proche une fois jouée), Ralentisseur, Désactivation de la visée adverse, Faire exploser sa propre bille (comptée empochée), Mélanger toutes les billes, Bille interdite à toucher (faute si touchée), Trajectoire de bille courbée en malus adverse, Commandes inversées pour l'adversaire, Bille traversant les billes adverses sans faute (mais pas les siennes).
- Ajout en TODO : prévoir d'autres modes de jeu avec objectifs propres, au-delà des 4 actuels (8-ball/9-ball/14.1/Party) — pas encore précisé.
